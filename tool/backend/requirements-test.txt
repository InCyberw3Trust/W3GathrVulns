# Minimal dependencies for running tests locally.
# Versions unpinned / bumped to support Python 3.13 on the host.
# The Docker container uses Python 3.12 with the pinned requirements.txt.
fastapi>=0.115.0,<1.0.0
starlette>=0.40.0,<1.0.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-dateutil>=2.9.0
apscheduler>=3.10.4

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
